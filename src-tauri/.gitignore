# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Tauri updater signing private key — public key is fine to commit.
# Private key signs update bundles; losing it means we can't ship updates.
.tauri-keys
